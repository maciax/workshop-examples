---
- name: Install Apache
  hosts: all

  tasks:
  - name: Install apache2
    apt:
      name: apache2
      state: absent
  - name: Remove unwanted Apache2 packages from the system.
    apt:
      autoremove: yes
      purge: yes